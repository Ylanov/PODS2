# ─── Web framework ───────────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0

# uvicorn[standard] уже включает websockets и httptools — отдельно не нужны
uvicorn[standard]>=0.30.0,<1.0.0

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0,<3.0.0
psycopg2-binary>=2.9.0

# ─── Validation ───────────────────────────────────────────────────────────────
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# ─── Auth / Security ──────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0

# БАГ-ФИКС: bcrypt 4.x ломает passlib — оставляем проверенную комбинацию.
# https://github.com/pypa/passlib/issues/689
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# Нужен для OAuth2 form data (application/x-www-form-urlencoded)
python-multipart>=0.0.9

# ─── Document generation ──────────────────────────────────────────────────────
python-docx>=1.1.0

# ─── Migrations ───────────────────────────────────────────────────────────────
alembic>=1.13.0

# ─── Production server ────────────────────────────────────────────────────────
gunicorn>=22.0.0

# ─── Data parsing ─────────────────────────────────────────────────────────────
openpyxl>=3.1.0

# ─── Rate limiting (защита от brute-force на /login и DoS) ────────────────────
slowapi>=0.1.9

# ─── In-process TTL-кеш (справочники: positions, schedules, ...) ──────────────
# Вместо Redis — держим кеш в памяти воркера, TTL 5 минут. При нескольких
# воркерах gunicorn каждый держит свою копию (это ОК: справочники редкие).
cachetools>=5.3.0

# ─── QR-коды (модуль проф. подготовки) ────────────────────────────────────────
# segno — pure-Python без зависимостей (≈100 KB), умеет SVG/PNG/Eps.
# Альтернатива qrcode[pil] требует Pillow, что лишний bloat.
segno>=1.5.0

# ─── HTTP-клиент (oper_map proxy: Yandex tiles/geocoder + OSRM routes) ────────
# Сервер ходит за тайлами/геокодером сам через инет-сетевуху, в локалке
# у пользователей интернета нет. httpx используем как async-клиент с
# поддержкой timeout'ов — стандарт в FastAPI-экосистеме.
httpx>=0.27.0,<1.0.0

# ─── Vault + парсинг сертификатов (модуль crypto-keys) ───────────────────────
# hvac — официальный Python-клиент HashiCorp Vault. Используется в
# app/services/vault_client.py для хранения контейнеров КриптоПро в KV v2.
hvac>=2.0.0,<3.0.0

# cryptography — парсинг X.509 .cer файлов и Fernet-шифрование для
# fallback-режима (когда Vault не настроен). Уже косвенно в зависимостях
# через python-jose[cryptography], но фиксируем явно для прозрачности.
cryptography>=42.0.0